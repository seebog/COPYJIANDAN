<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:orientation="vertical"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.lession11_musicplayer.MainActivity">

    <Button android:id="@+id/startBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="clickBtn"
        android:text="播放" />
    <Button android:id="@+id/stopBtn"
        android:layout_width="wrap_content"
        android:onClick="clickBtn"

        android:layout_height="wrap_content"
        android:text="停止" />
    <Button android:id="@+id/pauseBtn"
        android:layout_width="wrap_content"
        android:onClick="clickBtn"
        android:layout_height="wrap_content"
        android:text="暂停" />
    <Button android:id="@+id/nextBtn"
        android:layout_width="wrap_content"
        android:onClick="clickBtn"
        android:layout_height="wrap_content"
        android:text="下一首" />
    <Button android:id="@+id/preivwBtn"
        android:layout_width="wrap_content"
        android:onClick="clickBtn"
        android:layout_height="wrap_content"
        android:text="上一首" />

</LinearLayout>
